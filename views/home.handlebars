<div class="row">
    {{#each channel}}
  <div class="col-sm-3">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">{{id}}</h5>
        <p class="card-text">{{name}}</p>
        <a href="#" class="btn btn-primary">Enter</a>
      </div>
    </div>
  </div>
  {{/each}}
</div>